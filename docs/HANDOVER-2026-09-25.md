# Session handover — 2026-09-25 (branch `m4d-live-shots`)

## What this session did

Design phase for the **ball follow cam + raised viewpoint + range mat** — no code changed, two documents produced and committed:

1. **FOV/camera research** (@librarian + code verification): current rig was 1.7 m eye height / 4.5 m back ≈ 49°×29° effective FOV — narrower than the Unity/GSPro 60°-vertical norm; sim/broadcast range framings use 5–15 m elevated cams; GSPro Follow Camera translates with the ball after impact. Driver apexes exit the top of frame from a low cam (the known M4d gotcha).
2. **Brainstorming with visual mockups** (browser companion, user walked through every decision):
   - Follow cam: 1.5 s delay (early-engage if the ball nears frame top) → 0.8 s blend → chase 20 m back / 2.5 m up → descent pitch-in → 45° overlook of the landing → 2.5 s hold → **cut** back to static.
   - Static viewpoint: 8 m high / 21.2 m back crane position (mockup C — tee stays at v≈0.377, just above the bottom edge).
   - Mat: mockup B2 — 2.5 × 2.5 m rubber base, ball 0.35 m from the right edge (right-hander).
   - Pitch-in at 45° was a user **must**; `LAND_PITCH_DEG` is the designated tuning knob.
3. **Spec** — `docs/superpowers/specs/2026-09-25-follow-cam-viewpoint-mat-design.md` (commits `ab11e96`, `da9daaf`), user-approved.
4. **Implementation plan** — `docs/superpowers/plans/2026-09-25-follow-cam-viewpoint-mat.md` (commit `0538fe9`), self-reviewed, complete code in every step.

## TODO for next session (priority order)

1. **Execute the plan** `docs/superpowers/plans/2026-09-25-follow-cam-viewpoint-mat.md`, Tasks 1→6 in order. It is self-contained: full file contents for every change, exact test commands, commit per task. Summary of the task graph:
   - **Task 1**: rewrite `PovProjector` → `RangeCamera(x, y, z, pitchRad)` data class + pitch math (`depth = dy·cosθ − dz·sinθ`, `v = −(dy·sinθ + dz·cosθ)/depth`; pitch 0 collapses to today's math), `CAM_HEIGHT_M = 8.0`, `CAM_BACK_M = 21.2`, `RangeCamera.STATIC`. Rewrite `PovProjectorTest` (old hand-computed values break by design). Temporary `@Deprecated` compat overloads keep the canvas compiling until Task 3.
   - **Task 2**: new pure `FollowCam` object — `cameraAt(shot, playFraction)` phase machine (static → blend → chase → descent → hold → snap-back; short shots skip straight to the overlook; early-engage scans samples against `EARLY_ENGAGE_V = -0.17`), `endFraction(shot) = 1 + LAND_HOLD_SEC/T`. New `FollowCamTest` (15 tests, synthetic parabolic shots).
   - **Task 3**: plumb per-frame `camera` param through `PovRangeCanvas` (default `STATIC`; dynamic horizon `v0Px − tan(pitch)·focalPx`; apex clamp computed against STATIC only; tee ball min 4 px radius); delete compat overloads.
   - **Task 4**: `RangeScreen` — `FollowCam.cameraAt` each frame, playback extends to `endFraction`, tracer/head capped at `min(fraction, 1)`, waiting tee ball returns only after snap-back. **Note the plan's exact declaration placement**: `endF`/`waiting`/`ballRadiusM` move ABOVE the haze block (Task 5's mat block needs `waiting` in scope).
   - **Task 5**: new `RangeMat` (pure geometry + B2 palette) + mat quads in `PovRangeCanvas` (after haze, before tracers; `MAT_MIN_DRAW_DEPTH_M = 6.0` clearance skip) + mat quad in `TopDownCanvas`.
   - **Task 6**: full `.\gradlew.bat build`, `:app:installDebug` on TB373FU, manual verification checklist (driver early-engage, wedge pitch-in, hold, cut, short chip, history during chase, top-down mat) — then mark TODO#2 below in the 2026-09-24 handover doc.
   - Execution approach: subagent-driven (fresh executor per task) or inline — implementer's choice; the plan doc has the required-subskill header for either.
2. **Manual tablet verification pass of M4d** (carried from 2026-09-24 TODO#1 — still open).
3. **M4d cleanups**: `TmpDbg.kt` stray debug file (verify unused, delete).
4. Later phases (per ROADMAP): topography via `RangeScene.groundHeight()`, custom combines, bag mapping.

## Environment / conventions (from AGENTS.md, still true)

- `$env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"` before every gradlew command; `.\gradlew.bat build` / `:app:testDebugUnitTest --tests "…"`; tablet = Lenovo TB373FU via `:app:installDebug`.
- `PovProjector` / `FollowCam` / `RangeMat` must stay pure Kotlin (no Android imports) — deterministic, JVM-testable.
- Physics goldens (`core/physics`) untouched by this plan.

## Gotchas for the implementer

- **`EARLY_ENGAGE_V = -0.17`** assumes the reference tablet aspect (horizon 0.30h, focal 1.1w, 2000×1250). If the framing constant ever changes, revisit.
- **Frank-blend geometry**: the chase target *moves* during the blend (spec-intended). If a test asserts camera monotonicity, use a shot still rising through the blend window (the plan's test does — don't switch it back to the falling-apex wedge).
- Compose leftovers from last session: `Dp * Int` fine, `Int * Dp` NOT; `MissingPermission` lint needs `@SuppressLint` at BLE call sites.
- The follow sequence is best judged on-device by the user: if 45° feels wrong, tune `FollowCam.LAND_PITCH_DEG` first, then `OVERLOOK_DIST_M` / `LAND_HOLD_SEC`.
- Plan checkbox style: tick `- [ ]` boxes in the plan doc as tasks complete (last plan wasn't ticked — this one should be).

## Repo state

- Branch `m4d-live-shots`, working tree clean, all docs committed (`ab11e96` spec, `da9daaf` spec fix, `0538fe9` plan). Not pushed this session.
- Brainstorm mockups persisted at `.superpowers/brainstorm/175-1790250692/content/` (viewpoint-heights.html option C, mat-styles-v2.html option B2) — reference only, not needed to implement.
